# Sentinel Scanner

This is the open-source scanning component of Sentinel.  
It checks running processes, window titles, and DLLs inside Roblox for known cheat signatures.  
It does not connect to the internet or read personal files.  

To build: `cargo build --release`  
To run: `cargo run`
